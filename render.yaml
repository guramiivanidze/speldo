# Render Blueprint - https://render.com/docs/blueprint-spec
# This file defines all services needed for the Splendor game

services:
  # ============================================
  # BACKEND - Django + Channels (WebSocket)
  # ============================================
  - type: web
    name: speldo-backend
    runtime: python
    region: oregon
    plan: starter
    rootDir: backend
    buildCommand: ./build.sh
    startCommand: daphne -b 0.0.0.0 -p $PORT splendor.asgi:application
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: FRONTEND_URL
        fromService:
          type: web
          name: speldo-frontend
          property: url
      - key: DATABASE_URL
        fromDatabase:
          name: speldo-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: speldo-redis
          property: connectionString
      - key: PYTHON_VERSION
        value: "3.12.0"

  # ============================================
  # FRONTEND - Next.js
  # ============================================
  - type: web
    name: speldo-frontend
    runtime: node
    region: oregon
    plan: starter
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: speldo-backend
          property: url
      - key: NEXT_PUBLIC_WS_URL
        value: wss://speldo-backend.onrender.com
      - key: NODE_VERSION
        value: "20"

  # ============================================
  # REDIS - For WebSocket channel layer
  # ============================================
  - type: redis
    name: speldo-redis
    region: oregon
    plan: starter
    ipAllowList: []

# ============================================
# DATABASE - PostgreSQL
# ============================================
databases:
  - name: speldo-db
    region: oregon
    plan: starter
    databaseName: speldo
    user: speldo
